services:    
  ollama:
    image: ollama/ollama:latest
    container_name: ollama
    ports:
      - "11434:11434"
    volumes:  
    - ${USERPROFILE}/Documents/Ollama_Data:/root/.ollama
    restart: unless-stopped